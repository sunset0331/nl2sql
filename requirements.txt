# For Vercel / Linux serverless: pip install -r requirements.txt on build
# fetches manylinux wheels so pydantic_core._pydantic_core loads correctly.
# Local dev: use uv sync (see README).
flask>=3.1.2
openai>=1.0.0
pydantic>=2.0
pydantic-core>=2.41.5
python-dotenv>=1.2.1
requests>=2.32.5
sqlparse>=0.5.5
